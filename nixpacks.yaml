
setup: python311, gcc

install:
  - python -m venv /opt/venv
  - . /opt/venv/bin/activate
  - pip install -r requirements.txt

start: python GeminiDiscordBot.py  # Replace `main.py` with the actual entry point of your bot
